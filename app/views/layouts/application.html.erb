<!DOCTYPE html>
<html>
<head>
  <title>
    <% if mobile_device? %>
      Reading
    <% else %>
      ✌ Reading – What you're reading. Not what you like. Not what you find interesting. Just what you're reading.
    <% end %>
  </title>
  <link rel="icon" href="/favicon.ico" type="image/x-icon">
  <link rel="shortcut icon" href="/favicon.ico" type="image/x-icon">
  <link rel="apple-touch-icon-precomposed" href="apple-touch-icon.png">
  <script> var ENVIRONMENT = '<%= Rails.env %>'; </script>
  <%= auto_discovery_link_tag(:rss, "#{request.request_uri + (params[:type] == 'list' ? '/list' : '')}.rss?t=#{current_user.token}") if !params[:type].nil? and logged_in? %>
  <%= stylesheet_link_tag 'http://yui.yahooapis.com/3.3.0/build/cssreset/reset-min.css' %>
  <%= stylesheet_link_tag 'application' %>
  <% if mobile_device? %><meta name="viewport" content="width=device-width; initial-scale = 1.0; maximum-scale=1.0; user-scalable=no"><% end %>
  <%= stylesheet_link_tag 'mobile', :media => 'only screen and (max-device-width: 480px)' %>
  <%= javascript_include_tag 'jquery.min', 'jquery_ujs', 'jquery.cookies', 'jquery.tmpl.min', 'application', 'user', 'hook', 'native' %>
  <!--[if IE]><%= javascript_include_tag 'ie/html5' %><![endif]-->
  <%= csrf_meta_tag %>
  <%= render :partial => 'layouts/ga_embed' %>
  <script><%= render :partial => 'layouts/current_user_js' %></script>
</head>
<body>
  <div id="wrapper">
  <%= render :partial => 'layouts/header' if params[:action] != 'visit' %>
  <%= yield %>
  <%= render :partial => 'layouts/footer' if params[:action] != 'visit' %>
  </div>
  <script>
    <%= render :partial => 'posts/post.js.tmpl' %>
  </script>
  <%= render :partial => 'layouts/pusher' if logged_in? and @channels %>
</body>
</html>
