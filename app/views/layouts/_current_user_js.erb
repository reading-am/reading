var current_user_seed = <%=j (signed_in? ? render_api(partial: 'users/user', user: current_user) : {id: nil, username: '', display_name: ''}).to_json.html_safe %>
var authorizations_seed = <%=j !@authorizations.blank? ? render_api('authorizations/index', authorizations: @authorizations).to_json.html_safe : '[]' %>;
