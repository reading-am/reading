if(typeof window.fluid != 'undefined'){ (function(fluid){

$(function(){ $('head').append('<link rel="stylesheet" href="stylesheets/fluid.css" type="text/css">'); });

window.document.title = '✌ Reading';

// fluid.dockBadge = "10";

/*
fluid.showGrowlNotification({
    title: "title", 
    description: "description", 
    priority: 1, 
    sticky: false,
    identifier: "foo",
    // onclick: callbackFunc,
    // icon: imgEl // or URL string
});
*/

})(window.fluid); }

// prism
// window.platform.icon().badgeText = "33";
// window.platform.showNotification('Hey I work!', 'I work so freaking good');
