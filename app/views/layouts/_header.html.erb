<%
  if @user.nil? or (current_user && current_user.id == @user.id)
    you = "you"
    youre = "you're"
  else
    you = @user.first_name
    youre = "#{you} is"
  end
%>
<h1>&#9996; Reading</h1>
<h3>What <%= youre %> reading. Not what <%= you %> like<%= 's' if @user %>. Not what <%= you %> find<%= 's' if @user %> interesting. Just what <%= youre %> reading.</h3>
<div id="welcome">
<% if current_user %>
  Welcome <%= current_user.name %>
  <%= link_to "Log Out", signout_path %>
<% else %>
  Sign up or in with <%= link_to "Twitter", "/auth/twitter" %> or <%= link_to "Facebook", "/auth/facebook" %>.
<% end %>
</div>
