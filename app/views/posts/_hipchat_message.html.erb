<%= link_to @post.user.display_name, "http://reading.am/#{@post.user.username}" %> is <%= @post.page.domain.verb %> <%= link_to (@post.page.title.blank? ? @post.page.url : @post.page.title), @post.wrapped_url %><% if @post.referrer_post and @post.user != @post.referrer_post.user %> because of <%= link_to @post.referrer_post.user.display_name, "http://reading.am/#{@post.referrer_post.user.username}" %><% end %>
