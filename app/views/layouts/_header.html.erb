<%
  if @user.nil? or (current_user && current_user.id == @user.id)
    you = "you"
    youre = "you're"
  else
    you = @user.first_name
    youre = "#{you} is"
  end
%>
<h1>✌ Reading</a></h1>
<h3>What <%= youre %> reading. Not what <%= you %> like<%= 's' if @user and @user != current_user %>. Not what <%= you %> find<%= 's' if @user and @user != current_user %> interesting. Just what <%= youre %> reading.</h3>
<div id="welcome">
<% if current_user %>
  <% if current_user.username %>
    Hey there <%= link_to current_user.first_name || current_user.username, "/#{current_user.username}", :class => 'user' %>. 
    <%= link_to "Settings", "/#{current_user.username}/edit" %> | <%= link_to "Sign Out", signout_path %>
  <% end %>
<% else %>
  Sign up or in with <%= link_to "Twitter", "/auth/twitter", :class => 'external' %> or <%= link_to "Facebook", "/auth/facebook", :class => 'external' %>.
<% end %>
</div>
