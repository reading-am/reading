<div id="content">
  <h4>Some people are reading this stuff</h4>
  <table>
  <% @posts.each do |post| %>
    <tr>
      <td><%= post.user.display_name %></td>
      <td><%= link_to post.page.title, post.page.url, :class => 'external' %></td>
    </tr>
  <% end %>
  </table>
</div>
