<%= @enactor.name %> mentioned you in a comment:

<%= @comment.body.html_safe %>

++++++++

Respond at <%= @url %>

Or simply reply to this email.

--------
<% if @subject.is_adhoc? %>
<%= @subject.is_adhoc? ? @enactor.first_name : @enactor.display_name %> uses Reading.am to share links with friends, like you!
You should sign up too: <%= "#{ROOT_URL}#{new_user_session_path}" %>
<% else %>
No mas? http://<%= DOMAIN %>/settings/info
<% end %>
