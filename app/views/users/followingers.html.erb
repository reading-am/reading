<%=
  locals = mustache_helpers
  if params[:type] == "followers"
    locals[:section_followers] = true
  else
    locals[:section_following] = true
  end

  render :file => 'users/followingers/template', :locals => locals
%>

<%= render :partial => 'backbone/router', :locals => {:model => @user, :collection => collection} %>
