This is your Reading Digest
----------

<% @posts.group_by(&:page).each do |page, subposts| %>
## <%= page.display_title %>
<%= subposts.first.wrapped_url %>

<% end %>

---------
Prefer not to get a digest? http://<%= DOMAIN %>/settings/info
