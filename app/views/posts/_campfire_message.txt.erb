<%= ("✌ I'm #{@post.page.domain.verb}#{' "' + @post.page.title + '"' if !@post.page.title.blank?} #{@post.wrapped_url}").html_safe %><%= if @post.referrer_post and @post.user != @post.referrer_post.user then " because of #{@post.referrer_post.user.display_name} (http://#{DOMAIN}/#{@post.referrer_post.user.username})" end %>
