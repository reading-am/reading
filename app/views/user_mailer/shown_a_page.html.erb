<!DOCTYPE html>
<html>
<head>
  <%= stylesheet_link_tag 'email/base' %>
  <%= stylesheet_link_tag 'app/base' %>
  <%= stylesheet_link_tag 'models/user' %>
  <%= stylesheet_link_tag 'models/page' %>
  <%= stylesheet_link_tag 'email/overrides' %>
  <%= stylesheet_link_tag 'email/mentioned' %>
</head>
<body>

  <div class="row">
    <div class="span6">
      <p>
        <%= link_to "http://#{DOMAIN}/#{@enactor.username}", :class => 'r_user' do %>
          <span class="r_name"><%= @enactor.display_name %></span>
          <% if !@enactor.name.blank? %>
            <span class="r_username">(@<%= @enactor.username %>)</span>
          <% end %>
        <% end %>
        wants to show you a page:
      </p>
    </div>
  </div>

  <div class="row">
    <div class="span6">
      <%= render :partial => 'pages/page', :locals => { :page => @comment.page, :post => @comment.post, :wrap_url => true } %>
    </div>
  </div>

  <%= render :partial => 'footer' %>

</body>
</html>
