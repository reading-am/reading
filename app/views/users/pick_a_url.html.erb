<div class="row" id="pick_a_url">
  <div class="span7 offset1">
<%= twitter_bootstrap_form_for current_user, :html => {:class => "form-horizontal"}, :url => '/pick_a_url' do |f| %>
  <h3>We're almost ready, just pick a screen name.</h3>
  <%= f.text_field :username, "#{DOMAIN}/", :placeholder => 'i_live_here' do %>
  <% end %>
  <div class="actions" id="username_actions">
    <%= f.submit 'All done',:class => 'btn-success' %>
  </div>
<% end %>
</div>
</div>
