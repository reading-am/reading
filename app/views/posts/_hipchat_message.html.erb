<% token = false if token.nil? %>
✌ <%= link_to post.user.display_name, "http://#{DOMAIN}/#{post.user.username}" %> is <%= if !post.page.domain.nil? then post.page.domain.verb  else 'reading' end %> <%= link_to (post.page.title.blank? ? post.page.url : post.page.title), post.wrapped_url(token) %><% if post.referrer_post and post.user != post.referrer_post.user %> because of <%= link_to post.referrer_post.user.display_name, "http://#{DOMAIN}/#{post.referrer_post.user.username}" %><% end %>
