<script>
  var _pusher = {
    key: '<%= Pusher.key %>',
    channels: [
    '<%= if @channels.kind_of?(Array) then @channels.join("','") else @channels end %>'
    ]
  };
</script>
<%=
  javascript_include_tag(
    'http://rmsn.reading.am/nowjs/now.js',
    'http://js.pusherapp.com/1.9/pusher.min.js',
    'http://rmsn.reading.am/rmsn.js',
    'pusher'
  )
%>
