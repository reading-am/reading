<%=
  locals = {
    signed_in:          signed_in?,
    query:               params[:q],
    section_recommended: local_assigns[:section_recommended],
    section_expats:      local_assigns[:section_expats],
    section_search:      local_assigns[:section_search]
  }
  render file: 'users/find_people/template', locals: locals
%>

<%= render partial: 'backbone/router', locals: {router: :users, collection: collection} %>
