$.template('post', '<tr><td class="date"></td><td><div class=""><a href="http://reading.am/${user.username}" class="user">${user.display_name}</a> is reading <a href="${url}" class="external" data-base58_id="${base58.encode(id)}">${title}</a>{{if referrer_post.id}} because of <a href="http://reading.am/${referrer_post.user.username}" class="user">${referrer_post.user.display_name}</a>{{/if}}</div></td></tr>');
