$.template('post', '<tr><td class="date"></td><td><div class=""><a href="http://reading.am/${user.username}" class="user">${user.display_name}</a> is reading <a href="${url}" class="external" data-base58_id="${base58.encode(id)}">${title}</a></div></td></tr>');
