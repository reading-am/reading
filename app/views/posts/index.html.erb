<% if @user %>
  <%= render :partial => 'users/card', :locals => {:user => @user} %>
  <%= render :partial => 'users/you_subnav', :locals => {:user => @user} %>
  <%= render :partial => 'blank_slate' if @user.posts.size == 0 or @user.following.size == 0 %>
<% else %>
  <div id="subhead" class="row">
    <div class="span7 offset1">
    <% if @domain %>
      <h3>Stuff people are reading on <%= @domain.name %></h3>
    <% elsif signed_in? %>
      <h3>Posts from everyone on Reading</h3>
    <% else %>
      <h3>Some people are reading this stuff</h3>
    <% end %>
    </div>
  </div>
<% end %>

<%# render :partial => 'posts/group', :locals => {:posts => @posts} %>
<%= render template: 'backbone/templates/test.html.hbs', locals: { name: "David" } %>
<!-- 
<% if @user %>
<div id="new_post_row" class="row w_rule">
  <div class="span7 offset1">
    <form accept-charset="UTF-8" action="/api/posts" class="new_model" id="new_model" method="post">
      <div class="control-group" id="model_url_controls">
        <div class="controls">
          <input class="input-xlarge" id="model_url" name="model[url]" placeholder="Enter the url you'd like to post" size="30" type="text">
        </div>
      </div>
      <div class="form-actions">
        <input class="btn" name="commit" type="submit" value="Add Post">
      </div>
    </form>
  </div>
  <div class="span7 offset1">
    Even better, post using our <a href="/settings/extras">extension</a> or <a href="/settings/extras">bookmarklet</a>
  </div>
</div>
<% for @post in @posts %>
  <%= render template: 'backbone/templates/pages/page.hbs' %>
  <%= render template: 'backbone/templates/pages/page_row.hbs' %>
<% end %>


<% end %>
 -->


<%= render  template: 'backbone/templates/pages/page.hbs', locals: {url: 'http://google.com', title: 'Google', description: 'a fucking search engine.'} if @user %>
<%= render :partial => 'backbone/router', :locals => {:model => @user, :collection => @posts} if @user %>

<div id="posts_footer" class="row">
  <div class="span5 offset1">
    <%= will_paginate @posts, :page_links => false %>
  </div>
  <% unless params[:type].nil? %>
  <div class="span1">
    <%= link_to rss_path, {:class => 'rss'} do %>
      <span class="glyph">r</span> rss
    <% end %>
  </div>
  <% end %>
</div>