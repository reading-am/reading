<%= form_for hook, :html => {:class => 'form-inline'} do |f| %>
  <% if hook.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(hook.errors.count, "error") %> prohibited this hook from being saved:</h2>

      <ul>
      <% hook.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div id="constructor" class="hook">
    <span class="wrapper"></span>
    <%= f.submit "Thanks!", :class => "btn btn-success" %>
    <a href="#" class="footnote btn" data-url="/footnotes/campfire">Details</a>
  </div>
<% end %>
