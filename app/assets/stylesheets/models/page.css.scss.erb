.page a, .page.nope a span {
  color:#333;
  background:none;
}
.page.yep a span {
  color:#000;
}
.page.nope a {
  color:red;
  text-decoration:line-through;
}
.page .excerpt {
  color:#777;
  font-family:'Georgia', serif;
  font-style:italic;
  overflow:hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
}
