var current_user_seed = {
  id: <%= signed_in? ? current_user.id : "null" %>,
  username: '<%= current_user.username if signed_in? %>',
  display_name: '<%= current_user.display_name if signed_in? %>'
};

var authorizations_seed = <%=j signed_in? ? current_user.authorizations.map { |auth| auth.simple_obj }.to_json.html_safe : '[]' %>;
