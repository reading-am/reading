<!DOCTYPE html>
<html>
<head>
  <%= stylesheet_link_tag 'email/base' %>
  <%= stylesheet_link_tag 'app/base' %>
  <%= stylesheet_link_tag 'models/user' %>
  <%= stylesheet_link_tag 'comments/comment' %>
  <%= stylesheet_link_tag 'email/overrides' %>
  <%= stylesheet_link_tag 'email/mentioned' %>
</head>
<body>

  <div class="row">
    <div class="span6">
      <p>
        <%= link_to "http://#{DOMAIN}/#{@enactor.username}", :class => 'r_user' do %>
          <span class="r_name"><%= @enactor.display_name %></span>
          <% if !@enactor.name.blank? %>
            <span class="r_username">(@<%= @enactor.username %>)</span>
          <% end %>
        <% end %>
        mentioned you at <em><%= link_to @comment.page.display_title, @comment.post.wrapped_url, :class => 'page' %></em>:
      </p>
      <blockquote class="r_comment" cite="http://<%= DOMAIN %>/<%= @enactor.username %>/comments/<%= @comment.id %>">
        <div class="r_tri"></div>
        <p class="r_comment_body">
          <%= @comment.body_html.html_safe %>
        </p>
      </blockquote>
    </div>
  </div>

  <div class="row">
    <div class="span6">
      <%= link_to "Click to respond", @comment.post.wrapped_url, :class => "btn btn-success" %>
      or simply reply to this email.
    </div>
  </div>

  <%= render :partial => 'footer' %>

</body>
</html>
